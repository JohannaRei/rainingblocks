export default class Shape {
  constructor(type, color, id, left, top, right, bottom) {
    this.type = type;
    this.color = color;
    this.id = id;
    this.left = left;
    this.top = top;
    this.right = right;
    this.bottom = bottom;
  }
}
